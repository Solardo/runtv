var copyURLBtn = document.querySelector("	.js-url-copy-btn1	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/zPzKLvk/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn2	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/ChCFYDw/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn3	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/LhBYcZh/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn4	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/wYm450H/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn5	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/z7Z3pbP/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn6	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/18662Ld/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn7	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/JF6PdLk/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn8	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/BzZnth4/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn9	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/MB1YsKT/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn10	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/yd0jG8n/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn11	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/QkKPRbX/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn12	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/QbghD2p/2.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn13	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/cCqGcXq/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn14	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/jHVSD7N/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn15	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/k33W99q/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn16	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/vzPhprz/2.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn17	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/cvBbpN1/1.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn18	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/NCkG8mZ/2.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn19	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/TWTSjtd/3.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn20	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/CmW3xsN/4.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn21	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/rkpcGmL/5.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn22	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/F6rpFyx/6.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn23	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/LPjgpzD/7.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn24	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/RymX8D9/8.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn25	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/g6NmsHT/9.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn26	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/7JqZQmJ/10.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn27	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/h2dyXBV/11.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn28	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/yYJ888P/13.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn29	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/hfVJC0D/14.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
var copyURLBtn = document.querySelector("	.js-url-copy-btn30	");
copyURLBtn.addEventListener("click", function (event) {
  var id = "blah";
  var textarea = document.createElement("textarea");
  textarea.id = id;
  // Place in top-left corner of screen regardless of scroll position.
  textarea.style.position = "fixed";
  textarea.style.top = 0;
  textarea.style.left = 0;

  // Ensure it has a small width and height. Setting to 1px / 1em
  // doesn"t work as this gives a negative w/h on some browsers.
  textarea.style.width = "1px";
  textarea.style.height = "1px";

  // We don"t need padding, reducing the size if it does flash render.
  textarea.style.padding = 0;

  // Clean up any borders.
  textarea.style.border = "none";
  textarea.style.outline = "none";
  textarea.style.boxShadow = "none";
  // Avoid flash of white box if rendered for any reason.
  textarea.style.background = "transparent";
  document.querySelector("body").appendChild(textarea);
  console.log("The textarea now exists");
  existsTextarea = document.getElementById(id);
  existsTextarea.value = "	https://i.ibb.co/NpmS188/15.png	";
  console.log(existsTextarea.value);
  existsTextarea.select();
  var status = document.execCommand("copy");
  if (!status) {
    console.log("copy failed");
  } else
  {
    console.log("copy successful");
  }
});
